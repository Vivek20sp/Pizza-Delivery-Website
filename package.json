{
  "name": "pizza-delivery-app",
  "version": "1.0.0",
  "description": "Learning to make Pizza Delivery APP",
  "main": "index.js",
  "scripts": {
    "client-install": "cd client && npm install",
    "client-build": "cd client && npm run build",
    "install": "npm run client-install",
    "build": "npm run client-build",
    "server": "nodemon ./server/index.js",
    "pizzaapi": "nodemon ./PizzaAPI/index.js",
    "start": "concurrently \"npm run server\" \"npm run pizzaapi\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vivek Shankar Phadake",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
